import { useContext } from "react";
import { BooksContext } from "../../../context/books"

export default function Filter() {

    const { getListOfGenres } = useContext(BooksContext)
        
    return (<>
        <section>
            
        </section>  
    </>)
}